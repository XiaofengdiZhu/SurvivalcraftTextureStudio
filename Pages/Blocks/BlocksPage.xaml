<UserControl x:Class="SurvivalcraftTextureStudio.BlocksPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SurvivalcraftTextureStudio"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:BlocksPageViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:BlockTextureInfo}">
            <wpf:Card Width="120" Height="120" Margin="6">
                <Grid>
                    <wpf:PackIcon Width="120" Height="120" Kind="Cancel"
                                  Visibility="{Binding IsTextureExist, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"/>
                    <Image Width="120" Height="120" Source="{Binding Texture}"/>
                    <Border VerticalAlignment="Bottom" Background="#4C000000">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="0,0,10,0" Text="{Binding Index}"/>
                            <TextBlock Margin="0,0,10,0" Text="{Binding Name}"/>
                            <Button Content="点我" Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:BlocksPage}}, Path=DataContext.ChangeImageCommand, Mode=OneTime}" CommandParameter="{Binding}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </wpf:Card>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <DockPanel>
            <ScrollViewer Margin="0">
                <ItemsControl x:Name="wrap" Grid.Row="1" Margin="0,12,0,0"
                              ItemsSource="{Binding BlockTexturesValues, Mode=OneWay}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
        <Button Margin="32" HorizontalAlignment="Right" VerticalAlignment="Bottom" ToolTip="导出"
                Command="{Binding Path=ExportBlocksTextureCommand, Mode=OneTime}">
            <wpf:PackIcon Width="24" Height="24" Kind="Download"/>
        </Button>
    </Grid>
</UserControl>